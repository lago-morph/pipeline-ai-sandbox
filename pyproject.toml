[project]
name = "pipeline-ai-sandbox"
version = "0.0.0"
description = "Maintenance project for the agent-job protocol skills"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
