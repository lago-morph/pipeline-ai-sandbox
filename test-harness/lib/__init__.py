"""Shared infrastructure for test-harness scenario runners.

Modules:
    state            persistent state writer (state.json + console state block)
    archetype_loader materialises an archetype fixture into a temp dir
    assertions       phase assertion helpers
    scenario_runner  generic phase orchestration around a scenario YAML
"""
