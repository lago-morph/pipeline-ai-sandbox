[pytest]
testpaths = test-harness .claude/skills
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    live: requires live GitHub
    slow: takes > 30s
    archetype: archetype validation
    scenario: scenario runner test
